<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RakshaKavach">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TaskSelectionActivity" android:exported="false"/>
        <activity android:name=".PPEChecklistActivity" android:exported="false"/>
        <activity android:name=".IncidentReportActivity" android:exported="false"/>
        <activity android:name=".DailySafetyQuizActivity" android:exported="false"/>
        <activity android:name=".RiskAlertActivity" android:exported="false"/>

    </application>

</manifest>
